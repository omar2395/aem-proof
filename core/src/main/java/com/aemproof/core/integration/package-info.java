/**
 * External integrations. Every call to a system outside AEM goes through a service in this
 * package, so pages never depend directly on a remote API (see ADR-0002).
 */
@org.osgi.annotation.versioning.Version("1.0.0")
package com.aemproof.core.integration;
